export default function DeletePopup({  onCardDelete }) {
    
    
    return (
        <>
            <button className="popup__button" id="popup__button" onClick={onCardDelete}>si</button>           
        </>
        
    );
}